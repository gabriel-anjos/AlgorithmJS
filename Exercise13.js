const payment = 1250;
const paymentIncrease = 15 * payment / 100;
const newPayment = payment + paymentIncrease;
console.log(`Current payment: ${payment}.`);
console.log(`Payment with increase ajust: ${newPayment}.`)
