const width = 8;
const height = 9;
const area = width * height;
const ink = 2 ** 2;
const inkTotal = area / ink;
console.log(`The area of wall it's ${area} square maters. `)
console.log(`The wall will require ${inkTotal} liters of ink.`)