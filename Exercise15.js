const workDays = 30;
const hourPay = 20;
const workHour = 8;
const payment = (workHour * hourPay) * workDays;
console.log(`Work days: ${workDays}.`)
console.log(`Work hour per day: ${workHour}.`)
console.log(`The month payment it's: R$ ${payment}.`)