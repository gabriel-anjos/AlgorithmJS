const number1 = 8;
const number2 = 3;

if (number1 == number2) {
    console.log(`The values are equals!`)
} else {
    if (number1 < number2) {
        console.log(`The seconde value it's bigger.`)
    } else {
        console.log(`The first value it's bigger.`)
    }
}